name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  ubuntu:
    strategy:
      fail-fast: false
      matrix:
        build_type: [Debug, Release]
        compiler:
          - { name: gcc, cc: gcc, cxx: g++ }
          - { name: clang, cc: clang, cxx: clang++ }

    runs-on: ubuntu-latest
    env:
      CC: ${{ matrix.compiler.cc }}
      CXX: ${{ matrix.compiler.cxx }}

    steps:
    - uses: actions/checkout@v4
    - name: install dependencies
      run: |
        sudo apt update
        sudo apt install -y libxmu-dev libx11-dev libxext-dev \
          libxrandr-dev libxcursor-dev libxi-dev libxss-dev libxtst-dev \
          libfreetype-dev libharfbuzz-dev clang
    - name: build and install SDL3
      # ubuntu-latest (24.04 as of writing) has no libsdl3-dev package; SDL3
      # only landed in Ubuntu starting with 25.04. Build the pinned stable
      # release from source instead of depending on a distro package.
      run: |
        curl -fsSL -o /tmp/SDL3.tar.gz \
          https://github.com/libsdl-org/SDL/releases/download/release-3.4.12/SDL3-3.4.12.tar.gz
        tar -xzf /tmp/SDL3.tar.gz -C /tmp
        cmake -B /tmp/SDL3-build -S /tmp/SDL3-3.4.12 -DCMAKE_BUILD_TYPE=Release
        cmake --build /tmp/SDL3-build -j$(nproc)
        sudo cmake --install /tmp/SDL3-build
        sudo ldconfig
    - name: build and install SDL3_ttf
      # Same story as SDL3 above: no libsdl3-ttf-dev package on ubuntu-latest
      # yet. freetype/harfbuzz themselves come from apt (installed above),
      # so this only needs to build the thin SDL3_ttf layer itself.
      run: |
        curl -fsSL -o /tmp/SDL3_ttf.tar.gz \
          https://github.com/libsdl-org/SDL_ttf/releases/download/release-3.2.2/SDL3_ttf-3.2.2.tar.gz
        tar -xzf /tmp/SDL3_ttf.tar.gz -C /tmp
        cmake -B /tmp/SDL3_ttf-build -S /tmp/SDL3_ttf-3.2.2 -DCMAKE_BUILD_TYPE=Release -DSDLTTF_VENDORED=OFF
        cmake --build /tmp/SDL3_ttf-build -j$(nproc)
        sudo cmake --install /tmp/SDL3_ttf-build
        sudo ldconfig
    - name: configure
      run: cmake -B build -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
    - name: build
      run: cmake --build build -j$(nproc)
    - name: test
      run: ctest --test-dir build --output-on-failure --label-exclude long -j$(nproc)

  sanitizers:
    # ASan + UBSan leg: build the kernel and skelconq with
    # -fsanitize=address,undefined on RelWithDebInfo and run the quick ctest
    # lane (the same headless skelconq runs the other legs use) under the
    # sanitizers.  GCC is used because it is what reproduces locally; Clang
    # would work identically.  The UI is left off: the test lane only drives
    # skelconq, so building sdlconq under the sanitizers would add build
    # time (incl. SDL3-from-source) without adding sanitizer coverage.
    runs-on: ubuntu-latest
    env:
      CC: gcc
      CXX: g++
      # detect_leaks=0: the kernel allocates via xmalloc and frees almost
      # nothing by design, so LSan would drown the real signal.  Leak checking
      # is deferred until the planned de-globalization gives the kernel a
      # teardown path (MODERNIZATION-PLAN.md §9).  abort_on_error makes any
      # sanitizer finding raise SIGABRT so test/common.sh's crash check catches
      # it for every module, playable or not.
      ASAN_OPTIONS: detect_leaks=0:abort_on_error=1
      # halt_on_error=1: UB findings fail the run rather than being logged and
      # skipped past.
      UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1:abort_on_error=1

    steps:
    - uses: actions/checkout@v4
    - name: install dependencies
      run: |
        sudo apt update
        sudo apt install -y libasan8 libubsan1
    - name: configure
      # RelWithDebInfo: optimized enough that the 2-5x sanitizer slowdown stays
      # tolerable, still -g for readable reports.  TIMEOUT_SCALE widens the test
      # timeouts (CTest properties and test/common.sh's per-game bound) for the
      # slower sanitized runs, this build only.
      run: >
        cmake -B build
        -DCMAKE_BUILD_TYPE=RelWithDebInfo
        -DXCONQ_SANITIZE=address,undefined
        -DXCONQ_TEST_TIMEOUT_SCALE=3
        -DXCONQ_UI_SDL=OFF
    - name: build
      run: cmake --build build -j$(nproc)
    - name: test
      run: ctest --test-dir build --output-on-failure --label-exclude long -j$(nproc)

  fuzz:
    # Smoke-fuzz the GDL reader: build the opt-in libFuzzer harness
    # (test/fuzz/, -DXCONQ_FUZZ=ON, Clang only) and run it for a few minutes
    # over the checked-in corpus under ASan+UBSan.  This is a regression guard
    # for the reader bugs the fuzzer found (see test/fuzz/README.md) -- the
    # corpus-replay CTest re-parses every seed, and a short live run keeps
    # mutating from them.  Longer campaigns are a local/OSS-Fuzz job, not CI.
    runs-on: ubuntu-latest
    env:
      CC: clang
      CXX: clang++
      # Same rationale as the sanitizers leg: the reader never frees, so leak
      # detection is deferred; any ASan/UBSan finding must abort so libFuzzer
      # reports it as a crash.
      ASAN_OPTIONS: detect_leaks=0:abort_on_error=1
      UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1

    steps:
    - uses: actions/checkout@v4
    - name: install dependencies
      run: |
        sudo apt update
        sudo apt install -y clang
    - name: configure
      run: >
        cmake -B build
        -DCMAKE_BUILD_TYPE=RelWithDebInfo
        -DXCONQ_FUZZ=ON
        -DXCONQ_UI_SDL=OFF
    - name: build
      run: cmake --build build -j$(nproc) --target fuzz_gdl
    - name: corpus regression replay
      # -runs=0 executes every checked-in seed once and exits nonzero on any
      # crash; this is the fuzz-labelled CTest.
      run: ctest --test-dir build --output-on-failure -L fuzz
    - name: smoke fuzz
      # A short live run (~5 min) mutating from the checked-in corpus with the
      # GDL keyword dictionary; -runs caps it so a fast machine still stops.
      run: >
        ./build/test/fuzz/fuzz_gdl
        -dict=test/fuzz/gdl.dict
        -rss_limit_mb=4096
        -max_total_time=300
        -runs=20000000
        test/fuzz/corpus

  macos:
    runs-on: macos-latest
    # sdlconq no longer links X11/Xlib at all (it never actually called any
    # Xlib/Xmu/Xext API; that link line was vestigial from the SDL1.2->2->3
    # migrations) and no longer requires XQuartz to build or run -- see the
    # native-macos branch history. continue-on-error stays on until this job
    # has a longer track record of being green.
    continue-on-error: true

    steps:
    - uses: actions/checkout@v4
    - name: install dependencies
      # sdl3 is the SDL UI itself (not the old sdl12-compat shim -- see
      # MODERNIZATION-PLAN.md §4); sdl3_ttf is optional (real TrueType font
      # rendering -- see sdl/sdlscreen.cc's draw_string()) and falls back to
      # a built-in bitmap font if absent, so it's installed here for full
      # coverage but isn't load-bearing.
      run: brew install sdl3 sdl3_ttf
    - name: configure
      # Homebrew's prefix (/opt/homebrew on Apple Silicon runners) isn't on
      # CMake's built-in search path, so find_package(SDL3) needs a hint.
      run: cmake -B build -DCMAKE_PREFIX_PATH=$(brew --prefix)
    - name: build
      run: cmake --build build -j$(getconf _NPROCESSORS_ONLN)
    - name: test
      run: ctest --test-dir build --output-on-failure --label-exclude long -j$(getconf _NPROCESSORS_ONLN)
